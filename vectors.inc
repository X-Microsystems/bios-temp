; ---------------------------------------------------------------------------
; vectors.inc
; Imports for vectors.s
; ---------------------------------------------------------------------------

.import vt_init							                		;Initialization procedure
.import IRQ0, IRQ1, IRQ2, IRQ3, IRQ4, IRQ5, IRQ6, IRQ7, NMI_VECTOR, DMA_VECTOR, BRK_VECTOR	;Code labels
